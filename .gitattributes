# Windows 一键脚本的编码 / 换行规则 —— 别删，删了双击 .bat 会刷一屏
# 「'xxx' 不是内部或外部命令」。
#
# 背景：cmd.exe 是【一边执行一边按字节重读文件】的，它用「当前控制台代码页」
# 去解释这些字节。双击 .bat 时控制台的代码页是系统 ANSI，中文 Windows 上是 936(GBK)。
# 由此有两条硬性要求：
#
#   1) 换行必须是 CRLF。只有 LF 的话 cmd 会把好几行粘成一行，
#      括号块、goto、rem 注释全部错位。
#   2) 文件编码必须是 GBK(936)，和控制台代码页一致。
#      —— 注意不能靠在文件开头写 chcp 65001 来救 UTF-8 文件：cmd 记的是
#      字节偏移，文件读到一半改代码页会算错位置，长中文行会被从中间截断
#      （表现为「'看到' 不是内部或外部命令」这种半句话报错）。
#      所以是文件本身存成 GBK，而不是靠 chcp 转。
#
# 下面这行保证不管在哪台电脑上 clone、或者双击「一键更新.bat」拉代码，
# .bat 落到磁盘上永远是 CRLF。GBK 编码则靠仓库里存的就是 GBK 字节来保证
# （GBK 的第二字节取值范围不含 0x0A，所以 git 的换行处理不会破坏中文）。
*.bat text eol=crlf

# 反过来，macOS/Linux 的脚本必须是 LF + UTF-8。带上 CR 会报 bad interpreter。
*.sh      text eol=lf
*.command text eol=lf
